<?xml version="1.0" encoding="utf-8" ?>

<template>
	<params>
		<!-- Parameter value is not used, but an argument is passed by application layouts that is ignored here. -->
		<param name="mainTabbarName" value=""/>
		
		<param name="mainTabbar"/>
	</params>

	<layout name="Content"
		resizable="false"
	>
		${mainTabbar}
		<layout-reference resource="statusBar.layout.xml"/>
	</layout>
</template>