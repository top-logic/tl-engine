<?xml version="1.0" encoding="utf-8" ?>

<view>
	<channels>
		<channel name="model"/>
	</channels>
	<window width="380px">
		<title>
			<en>Login</en>
			<de>Anmeldung</de>
		</title>
		<form input="model"
			initial-edit-mode="true"
			mode-switch="false"
		>
			<field attribute="name"/>
			<field attribute="password"/>
			<!--
				Extra login commands contributed by optional feature modules (e.g. self-service
				"Forgot password?", SSO providers) via the LoginCommandsConfig application config.
				Renders nothing when nothing is contributed.
			-->
			<login-commands/>
			<commands>
				<generic-command placement="BUTTON_BAR">
					<label>
						<en>Login</en>
						<de>Anmeldung</de>
					</label>
					<store-form-state/>
					<login/>
				</generic-command>
			</commands>
		</form>
		<actions>
			<button>
				<action class="com.top_logic.layout.view.command.CancelDialogCommand">
					<label>
						<en>Cancel</en>
						<de>Abbrechen</de>
					</label>
				</action>
			</button>
		</actions>
	</window>
</view>
