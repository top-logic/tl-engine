/**
 * A Collections of tags that cooperates with the Layout.
 * <p>
 * We redefine the some html tags (html, head, body, form) and intodruce the new one ("action") so
 * cooperation with the layout works as intended.
 * </p>
 */
package com.top_logic.mig.html.layout.tag;
