<?xml version="1.0" encoding="utf-8" ?>

<template>
	<params>
		<param name="renderer"
			optional="true"
		/>
		<param name="entryRenderer"
			optional="true"
		/>
	</params>

	<!-- Included by default for compatibility reasons. This complete file is overridden in module tl-model-search. -->
	<include name="toolrow/searchButtonsLegacy.xml"/>
</template>