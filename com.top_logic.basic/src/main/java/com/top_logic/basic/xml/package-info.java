/**
 * Utility classes to support parsing and creating of XML.
 * <p>
 * A former requirement where based on the need for a successor to pax
 * (<a href="http://www.daedalos.com/DE/DOWNLOAD/PAX">pax</a> which uses lots of now deprecated
 * methods. Later utility classes to parse XML via SAX and to write XML in a structured way where
 * added.
 * </p>
 */
package com.top_logic.basic.xml;
