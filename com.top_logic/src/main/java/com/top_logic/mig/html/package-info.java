/**
 * 	Create HTML from Swing based models and classes.
 * <p>
 * 	You cannot expect a 1:1 mapping of the swing classes
 * 	to HTML: You can however manipulate the classes and
 * 	then (re-)render them as HTML. To Do so call 
 * 	<code>getHTML()</code> after bringing the Objects into
 * 	the desired state.
 * </p>
 */
package com.top_logic.mig.html;
