<?xml version="1.0" encoding="utf-8" ?>

<template>
	<params>
		<param name="renderer"
			optional="true"
		/>
		<param name="entryRenderer"
			optional="true"
		/>
	</params>

	<group name="search">
		<views>
			<include name="toolrow/searchButtons.xml"
				entryRenderer="${entryRenderer}"
				renderer="${renderer}"
			/>
		</views>
	</group>

	<group name="help">
		<views>
			<include name="toolrow/helpButton.xml"
				entryRenderer="${entryRenderer}"
				renderer="${renderer}"
			/>
		</views>
	</group>
</template>