The tests in this package test features of the scripting framework itself.

The tests need a running web-application. This is the reason that the tests 
don't lie in com.top_logic but in com.top_logic.demo, because there is no web 
application in com.top_logic. 