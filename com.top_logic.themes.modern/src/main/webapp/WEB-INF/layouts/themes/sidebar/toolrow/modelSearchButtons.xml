<?xml version="1.0" encoding="utf-8" ?>

<template>
	<params>
		<param name="renderer"
			optional="true"
		/>
		<param name="entryRenderer"
			optional="true"
		/>
	</params>
	<include name="model/search/modelSearchButton.xml"
		labelKey="layout.model.search.tabber"
		renderer="${renderer}"
		tabName="modelSearch"
		viewName="modelSearchTab"
	/>
	<!-- Model search buttons are components and displayed in the sidebar anyways. -->
</template>