<?xml version="1.0" encoding="utf-8" ?>
<view xmlns:config="http://www.top-logic.com/ns/config/6.0">
  <channels>
    <channel name="selected" />
  </channels>
  <split-panel>
    <start>
      <panel title="Flow Diagram">
        <flow-diagram selection="selected" createChart="chart(
          tree(
            list(
              border(padding(text('Parent'), all: 5)),
              border(padding(text('Child A'), all: 5)),
              border(padding(text('Child B'), all: 5))
            ),
            list(
              connection(1, 2),
              connection(1, 3)
            )
          )
        )" />
      </panel>
    </start>
    <end>
      <panel title="Selection">
        <!-- The selected channel value would be displayed here when connected to a form or table. -->
      </panel>
    </end>
  </split-panel>
</view>
